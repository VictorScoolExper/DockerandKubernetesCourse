apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  # the selector tells it what set of pods it is suppose to create access for
  selector:
    component: web
  ports: 
    - port: 3000
      targetPort: 3000
  